import { Settings as SettingsIcon } from 'lucide-react'
import DashboardPage from '../DashboardPage'

export default function Settings() {
  return <DashboardPage icon={SettingsIcon} title="Settings" description="Configure your campus profile, notifications, and staff preferences." />
}
